language: csharp
sudo: required
dotnet: 2.0.3

matrix:
  include:
    - os: linux # Ubuntu 14.04
      dist: trusty

branches:
  only:
    - master

before_script:
  - chmod a+x ./build.sh
script:
  - ./build.sh
